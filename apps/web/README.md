# TraFriend Web

Next.js frontend for the TraFriend Phase 1 mock workspace.

Use the repository's [`docs/DEVELOPMENT.md`](../../docs/DEVELOPMENT.md) for installation, local startup, environment variables, and verification commands.

Market-data credentials must never be added to this application or to a `NEXT_PUBLIC_*` variable.

